{
    "name": "artifactory-vsts-extension",
    "version": "1.0.0",
    "dependencies": {
        "babel": "^6.23.0",
        "babel-core": "^6.26.3",
        "tfx-cli": "^0.5.14"
    },
    "scripts": {
        "install": "node build.js && tfx extension create --manifest-globs vss-extension.json",
        "test": "node build.js && npm t --prefix=tests"
    },
    "license": "Apache-2.0"
}
